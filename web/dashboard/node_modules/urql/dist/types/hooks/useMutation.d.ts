import { DocumentNode } from 'graphql';
import { TypedDocumentNode, OperationResult, OperationContext, CombinedError, Operation } from '@urql/core';
export interface UseMutationState<Data = any, Variables = object> {
    fetching: boolean;
    stale: boolean;
    data?: Data;
    error?: CombinedError;
    extensions?: Record<string, any>;
    operation?: Operation<Data, Variables>;
}
export declare type UseMutationResponse<Data = any, Variables = object> = [
    UseMutationState<Data, Variables>,
    (variables?: Variables, context?: Partial<OperationContext>) => Promise<OperationResult<Data, Variables>>
];
export declare function useMutation<Data = any, Variables = object>(query: DocumentNode | TypedDocumentNode<Data, Variables> | string): UseMutationResponse<Data, Variables>;
