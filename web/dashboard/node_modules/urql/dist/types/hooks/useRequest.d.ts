import { DocumentNode } from 'graphql';
import { TypedDocumentNode, GraphQLRequest } from '@urql/core';
/** Creates a request from a query and variables but preserves reference equality if the key isn't changing */
export declare function useRequest<Data = any, Variables = object>(query: string | DocumentNode | TypedDocumentNode<Data, Variables>, variables?: Variables): GraphQLRequest<Data, Variables>;
