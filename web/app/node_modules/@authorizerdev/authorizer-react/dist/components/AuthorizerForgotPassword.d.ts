import { FC } from 'react';
import { Views } from '../constants';
export declare const AuthorizerForgotPassword: FC<{
    setView?: (v: Views) => void;
    onForgotPassword?: (data: any) => void;
    onPasswordReset?: () => void;
    urlProps?: Record<string, any>;
}>;
