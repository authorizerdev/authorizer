import { FC } from 'react';
import { Views } from '../constants';
export declare const AuthorizerVerifyOtp: FC<{
    setView?: (v: Views) => void;
    onLogin?: (data: any) => void;
    email?: string;
    phone_number?: string;
    urlProps?: Record<string, any>;
    is_totp?: boolean;
}>;
