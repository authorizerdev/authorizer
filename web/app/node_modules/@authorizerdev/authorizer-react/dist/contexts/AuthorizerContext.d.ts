import React, { FC } from 'react';
import { AuthorizerContextPropsType, AuthorizerState } from '../types';
export declare const AuthorizerProvider: FC<{
    children: React.ReactNode;
    config: {
        authorizerURL: string;
        redirectURL: string;
        clientID?: string;
    };
    onStateChangeCallback?: (stateData: AuthorizerState) => Promise<void>;
}>;
export declare const useAuthorizer: () => AuthorizerContextPropsType;
